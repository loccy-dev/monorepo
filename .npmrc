# Hoist to root node_modules so Nuxt/Nitro (apps/web, private) resolves a single
# `vue` realm — else rollup dedupes inconsistently and SSR crashes ("reading 'ce'").
# Refs: nuxt/nuxt#14146, nuxt/nuxt#28468, unovue/reka-ui#1239
shamefully-hoist=true
